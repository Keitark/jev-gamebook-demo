# Browser-use / Computer-use: 固定操作欄と頁の演出（v0.4）

## 更新して起動

実行中のサーバーを止めてから、リポジトリで実行します。既存の `.env` は変更しません。

```sh
git pull
python web_app.py --open
```

ブラウザを再読み込みしてください。設定の「選択肢の順序」で原順／偏り検証を切り替え、「この設定で新しく始める」を押します。ブラウザの新規プレイでは偏り検証が初期値です。既存APIの互換性のため、`POST /api/runs` で `choice_order` を省略した場合は原順です。

## 戦闘でボタンを見失わない

本文の長さや右側の戦闘ログが増えても、本と操作ボタンを下へ押し出さないレイアウトにしました。デスクトップでは左右の欄が独立スクロールし、右ページ下の行動欄は本文のスクロールから独立しています。長い本文は依然としてスクロールして読む必要があります。

日本語編の戦闘は、左から右、上から下へ次の8枠を固定します。

| 左 | 右 |
|---|---|
| 1. 攻撃 | 2. 精密攻撃 |
| 3. 防御 | 4. 回復薬 |
| 5. 清め布 | 6. 鳴塩を投げる |
| 7. 煙玉 | 8. 撤退 |

集中不足・道具切れ・撤退不可では、そのボタンが同じ位置で無効になります。使用できないボタンを消してほかを詰め直しません。Kai互換戦闘は「1ラウンド戦う」「Evasion」の2枠です。数字キーもこの画面上の枠に対応し、無効な枠では何もしません。

DOM操作では `data-action-id` または `data-choice` の行動ID、アクセシビリティ名を使えます。短縮表示したボタンも、`aria-label` と `title` に行動の全文を残しています。本文・状態は普通のDOMテキストです。

## ブラウザAIに渡す指示例

> このゲームブックを、本文と現在の状態を読んでプレイしてください。毎手、すべての有効な選択肢を比較し、先頭という理由では選ばないでください。長文は本文欄をスクロールして最後まで確認します。戦闘中は敵の予備動作、体力、集中、装備、回復手段を確認してください。選択後は丸囲みとページめくりが終わり、「次の一手」が再度有効になるまで待ち、改めて状態を取得します。古い画面の座標や前の節の選択肢番号を使い回さないでください。手動で判断して遊ぶ場合は、Randomを動かす「次の一手」ではなく、判断した行動ボタンを押してください。結末に達したら止めてください。全シナリオや未読の節を直接取得しないでください。

JavaScriptを使えるブラウザエージェントには `gamebook.observe()` もあります。

```js
const observation = gamebook.observe();
// observation.ready === true、observation.status === 'live' を確認する。
// observation.passage / character / combat / choices を読んでから決める。
```

`ready` は通信・書き込み演出・ページめくりの間は `false`。`phase` は `thinking` / `annotating` / `turning` / `ready` です。`#book` の `aria-busy` でも確認できます。`choices` は実行可能な行動だけで、画面上の無効枠は含みません。`run_id` と `revision` は、観測と操作を対応づけるために使います。

## 手書き％、丸囲み、音

判断が返ると、元の頁の選択肢へ赤インク風の数字を書き込み、選ばれた行動を約350msで丸く囲みます。短く見せてから頁をめくります。文字はオリジナルの単線SVG字形で、外部フォントの取得なしでも手書き風になります。演出の要素はクリックを遮りません。数字には読み取り用のテキストも併設します。

丸囲みには短い「スパッ」という合成音、連続めくりには各紙に同期した短い紙音を追加しました。既存の剣戟・回復・勝利音、音量・ミュートはそのままです。音は右上の「音 OFF」で有効化します。

**確率がないところには数字を作りません。**

- Jev / OpenRouter: 実際のAPI応答の確率。行動IDに対応づけます。
- Random: 本当の均等抽選確率。Jevと区別して表示します。
- 普通のクリック／強制継続: 丸のみ。架空の100%は付けません。
- ブラウザAIの明示的な自己申告: 自己申告と表示し、Jevのconfidenceとは区別します。

ブラウザAIが確率も渡す場合は、判断をした後に次のインターフェースを使えます。`chosenActionId` と `probabilitiesByActionId` はエージェントが決めた値です。

```js
await gamebook.chooseWithProbabilities({
  run_id: observation.run_id,
  revision: observation.revision,
  choice: chosenActionId,
  probabilities: probabilitiesByActionId
});
```

確率は、その観測の **全有効行動ID** をキーとするオブジェクトにし、各値0〜1、合計1にします。未観測の行動・欠けた行動・NaN・古いrevisionはサーバーで拒否します。関数は演出終了まで待って次の観測を返します。失敗時に自動で別の選択を実行しません。この経路は実行記録の `source: browser` / `probability_source: browser_self_report` として保存されます。

単なるマウスクリックから、ブラウザAIの内部確率を自動取得することはありません。自己申告の数値も校正されたクリア率ではありません。

## 「上ばかり選ぶ」を切り分ける

偏り検証モードでは、探索の表示順をseedと節から決定的に並べ替えます。同じ節でボタンが突然飛び回ることはありません。戦闘中の画面配置は上記の固定枠のままです。JevへのAPI入力順はこれとは独立に、各手で決定的に並べ替え、観測内の行動一覧とAPI候補の順序を一致させます。

行動ID、行動の効果、返ってきた確率、モデルが選んだ行動は変更しません。並べ替えはゲーム内のダイス乱数を消費しません。原順／偏り検証で同じseed・同じ行動列なら戦闘結果も同じです。ただしRandomの「順番を抽選する」結果は候補順が変わると変わり得るため、モデル比較には順序設定も記録してください。

左側に2択以上の判断に対する先頭選択数を表示します。JSON記録には次も追加しました。

```text
choice_order
trace[].presentation.display_order
trace[].presentation.provider_order
trace[].presentation.display_index
trace[].presentation.provider_index
trace[].probability_source
```

先頭選択数は有効行動を順に並べたときの位置で、無効な灰色枠は数えません。外部ブラウザAI自身の判断を強制的に別の選択へ変える機能ではなく、表示順の影響を観測するための機能です。

## ページ番号と前後のパラパラめくり

手数、節番号、頁番号を分離しました。頁番号は **このUIの仮想組版** です。原本の印刷頁を名乗りません。節を番号順に並べ、1節を1見開きへ固定配置し、表裏の表紙分を加えます。日本語60節なら122頁、最初の節は2–3頁です。番号に欠番がある本では、存在する節の順番を使います。

例: 日本語編の §3 は6–7頁、§7 は14–15頁。戻れば同じ頁番号に戻ります。戦闘ラウンド、道具使用、装備変更など同じ節の操作では頁は進みません。

先の頁へは右から左、戻るときは左から右へめくります。隣接なら1枚、離れた節なら少しずつ重なる複数枚。大きなジャンプは最大9枚で束を表現し、カウンターは実際の仮想頁の距離を示します。途中の紙は装飾線だけで、未読のシナリオを先読みして表示しません。

動きを控えめにする設定とスマホの1頁表示では、方向付きの短いフェードにします。描画・テストの範囲は [READER_POLISH_TESTING.md](READER_POLISH_TESTING.md) を参照してください。
