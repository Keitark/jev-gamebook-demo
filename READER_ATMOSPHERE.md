# 黒い活字・✔・長いページ送り — reader atmosphere layer

前回配布した `reader-atmosphere.patch` を、`fdc2b93515cf1dfae75ae008d567b0db3b6ac242` を基点としてリポジトリに統合する版です。

## 更新・起動

動作中のサーバーを止めて、リポジトリ内で実行します。

```powershell
git pull --ff-only
python web_app.py --open
```

ブラウザも完全に再読み込みしてください。`.env`、APIキー、シナリオ、戦闘ルール、バックエンドは変更しません。
先に同じパッチを手で適用した場合、ローカル変更や未追跡ファイルと衝突することがあります。強制リセットや削除はせず、変更を退避して確認してください。パッチの再適用は不要です。

## GitHub反映の再試行で確認したこと

- 取得した `main` は `fdc2b93515cf1dfae75ae008d567b0db3b6ac242`。差分の基点と一致。
- 対象 `web/index.html` のGit blob SHAは `9e28a58731f25b30ab173a267b147df23bb12c3b` と一致。パッチを適用し、追加の読込2行だけが変わることを確認。
- `git apply --check` が成功。追加JSは `node --check`、2つのPythonツールは `py_compile` が成功。
- JS・CSS・HTML・2つのツールを、配布パッチと同じバイト列のGit blobとして格納。説明書のみ統合後の起動手順に更新。
- この再試行ではブラウザテスト・ゲーム全体のテスト・有料API呼出しを再実行していません。下記の24項目は前回のパッケージ作成時の結果です。

## 内容

`web/index.html` にCSSとJSの読込を各1行追加し、`web/reader-atmosphere.js` / `.css` を読み込みます。
既存の固定戦闘枠、行動ID、並べ替え規則、確率、ゲーム内乱数、節と仮想ページの対応はそのままです。

- 確率: 太細のあるオリジナルSVGの黒文字風数字。必要な数値だけを描き、丸め方も従来の表示精度を維持します。本文とは別の殴り書きで、色は暗い赤。フォントファイルは含めません。
- 選択: 丸囲みではなく短い折れ線110msと長い跳ね145msの✔。跳ねに既存のslash音を同期させます。
- 間: 数字表示・判断の間・刻印後の1秒を含めて約2.1秒。線自体をだらだら描かず、選択結果を眺める時間を長くします。
- ページ: 従来の最大9枚から最大28枚の動く紙へ。方向と到着ページは本の実際の仮想頁に従います。長距離は4.8秒以内のアニメーション予定時間。テクスチャ準備時間は別途必要です。
- 負荷: CSS版は最大6枚の描画器を使い回し、同じ紙の画像化をキャッシュします。全28枚×56分割をDOMに同時展開しません。途中の紙は中立の装飾だけで未読の本文を表示しません。
- 操作: 装飾はクリックを奪いません。ボタンは固定位置。戦闘ラウンドなど同じ節ではページ送りをしません。
- 「動きを控えめに」では時間を短縮し、重い頁テクスチャ生成も省きます。

普通のクリックには確率が付いていないため、✔だけです。Jev応答／Random分布／明示的なブラウザAIの自己申告を区別し、確率を作りません。

## 約20秒の停止を切り分ける

元コードの `app.js` は操作ごとに `await audio.unlock()` し、`sound.js` の `unlock` は `await AudioContext.resume()` していました。
音声再開が許可されないとそのPromiseが未完了になり得るため、音を使う許可とゲームの実行が不必要に結びついていました。

この層は音声再開を非ブロッキングにし、鳴らせないときは黙ってゲームを進めます。許可後に古い効果音をまとめて再生することもありません。
ページ演出にも終了期限と後片付けを設け、描画失敗でサーバーが既に受理した行動を古い画面のままにしないようにしています。

ただし、ユーザー環境の20秒をこの音声問題と断定したものではありません。外部ブラウザAIの次の入力までの時間、API往復、描画時間は別です。

右上の新しい時間表示:

- **要求処理中**: 入力受理からサーバー応答・処理まで。
- **選択を刻んでいます / 頁を探しています**: UI演出。
- **次の操作待ち**: アプリは既に操作可能。外部AIまたは利用者の次の入力を待っています。

「待機ログ」でJSON保存できます。`ReaderAtmosphere.timing()` でも同じ内容を取得できます。
`previous_ready_ms` は前の操作可能時点から次の入力までの間隔で、モデルの推論時間と同一視しません。
`request_ms` / `annotation_ms` / `turn_ms` を分離し、既存プロバイダーの報告応答時間も別項目に残します。
診断ログにはAPIキー、プロフィール、本文を入れません。

Web Audio仕様: https://webaudio.github.io/web-audio-api/#dom-audiocontext-resume

## パッケージ作成時の検証範囲（前回の記録）

実行コマンド:

```powershell
python tools/check_reader_atmosphere.py --browser /usr/bin/chromium
```

**24項目成功・JavaScript例外0。** これは演出層の分離テストです。日本語表示の固定テストデータ、実PaperAudio、実CSS 3D、追加JSを使います。APIとアプリの進行部分は明示的なテスト用の接続口で、ライブモデルやバックエンド通しの確認ではありません。

パッケージ作成時に測った値（環境依存・保証値ではない）:

- 確率＋✔演出: 約2.11秒。
- 永久に完了しない音声resumeを与えても、unlockは約0.1msで復帰。
- 24枚のCSSページ送り: 描画器6個、テクスチャ準備約1.40秒、準備込み約6.13秒。
- RAFを意図的に止めても約1.85秒で1枚送りのロックを解除。

720p・768p・1080pで戦闘と長いログのボタン位置、手動では確率を作らないこと、前後方向、描画失敗後の状態確定、モバイルの横はみ出し等を確認しています。

**最新main全体の通しテスト、Jev/APIの実呼出し、Three.jsの実GPU動作は今回未検証です。**
テストのCSS3D基底クラスは会話添付の以前の実装で、追加層と公開インターフェースを検証しています。配布差分はその基底クラスを上書きせず、最新mainの既存クラスに追加されます。
以前の111件等の結果を今回の実行結果として扱わないでください。

## 動画

```powershell
python tools/capture_atmosphere.py --browser /usr/bin/chromium
```

14秒、1600×900、24fps、H.264/AAC。追加した演出を実ブラウザで撮影し、同じWeb Audioシンセサイザーの記録イベントから音を再生成します。
画面の確率・場面は**演出確認用の固定テスト値**で、Jevの推論や実ゲームプレイの録画ではありません。
