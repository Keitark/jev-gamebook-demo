# Copy to .env; fill only the provider you use. Never commit .env.
JEV_API_KEY=
OPENROUTER_API_KEY=

# Optional model overrides (the server reads these on restart).
# JEV_MODEL=jev-latest
# OPENROUTER_MODEL=typesafe/jev-1.13
