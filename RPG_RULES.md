# 灰鐘の港 — ルールとシナリオの定義

この作品用のオリジナルルールです。Fighting FantasyやLone Wolfの戦闘ルールではありません。画面の「?」からも要点を読めます。

## 最初の状態

体力30/30、集中3/4、技量4、敏捷3、銀貨9、潮位0/16。
欠けた短剣（威力2・命中補正0）と旅人の外套（装甲1）を装備し、回復薬2、携帯食2、清め布1、鳴塩の小瓶1を持っています。

**潮位はリアルタイムではありません。** コスト欄に書かれた移動・選択、本文中の危険で増減します。潮位16で時間切れ。体力0で死亡し、同時なら死亡を優先します。戦闘・装備変更・道具使用を含めて200行動で実験上の上限に達します。

## 一手の戦闘

先にプレイヤーの行動、その結果まだ生きている敵が行動します。逃走成功・敵撃破では反撃はありません。

- **通常攻撃:** `d6 + 技量 + 武器命中 + 準備補正 >= 敵回避` で命中。ダメージは `max(1, d6 + 武器威力 - 敵装甲)`。敵の「身を固める」中は敵回避+2。
- **精密攻撃:** 集中を2使い、必中・装甲無視で `武器威力 + 5` ダメージ。集中不足なら選択肢自体が出ません。
- **防御:** 今回の回避+2、被ダメージ-4、集中+2（最大4）。次の通常攻撃の命中+2。連続しても準備補正は重なりません。通常攻撃か精密攻撃をすると準備を使い切ります。
- **敵の命中:** `d6 + 敵技量 >= 7 + 敏捷//2 + 防御補正`。命中すると `max(0, d6 + 敵威力 + 大振り補正 - 自分の装甲 - 防御軽減)` のダメージ。大振り補正は+3。
- **道具:** 回復薬は体力+10、清め布は+6。回復は上限までで、使用した手も敵が行動します。鳴塩の小瓶は固定8・装甲無視。携帯食は戦闘外限定で体力+6・集中+1。
- **撤退:** 退路のある敵だけ `d6 + 敏捷 >= 8`。失敗すると敵が行動。煙玉は同じ退路へ確実に逃げます。逃げた敵に戦利品はなく、再戦時は敵が全快します。

敵の表示は「次に行う手」です。「大振りの予備動作」「体勢を立て直す」は攻撃しないため、回復の機会になります。装備変更は戦闘外だけで、買っただけでは装備されません。

## 探索

条件付き選択肢・購入・消費はサーバーで判定します。ブラウザやモデルが勝手に所持品を加えたり、HPを書き換えたりすることはできません。所持金・必要道具が不足した購入は状態も乱数も変えず拒否します。

拾得物・発見・勝利の報酬は一度きり。危険な水門を繰り返し操作した場合のダメージと潮位増加は繰り返し適用されます。戦闘に勝った敵は復活しません。通った場所は戻れる場合がありますが、すべての分岐が引き返せるとは限りません。

所持枠や重量の制限はありません。「原本と釣銭」の二者択一は重量制限ではなく、相手が両方を渡せないという物語上の制約です。再訪しても二つとも受け取れません。

## 別の本を作るためのデータ構造

正本は `story_ja.py`、書き出しは `python story_ja.py --output story-export`。
`RPGBook(spec)` に渡す `decision-gamebook/v1` JSON互換データは次の要素を持ちます。

```json
{
  "format": "decision-gamebook/v1",
  "id": "your-book",
  "version": "1.0.0",
  "title": "作品名",
  "language": "ja",
  "objective": "プレイヤーの目的",
  "start": "1", "death": "98", "deadline": "99", "tide_limit": 16,
  "initial": {"inventory": {}, "equipment": {}},
  "items": {}, "enemies": {}, "clues": {}, "sections": {}
}
```

これは構造例で、この空のままでは遊べません。`initial`で省略した能力値は `Hero` の初期値を使います。

各節には `title`, `text`（段落配列）, `choices` を定義し、戦闘節なら `combat`、終幕なら `ending` を追加します。選択肢は `id`, `text`, `to`。`requires`で条件、`effects`で変化、`once`で一度きり、`locked`で不足条件の表示、`hidden`で選べないとき非表示にできます。コロンを含むIDは道具・戦闘コマンド用に予約しています。

条件演算子は `all`, `any`, `not`, `flag`, `clue`, `item`, `gold`, `tide_max`, `tide_min`。複数のキーはANDです。`item`は1個以上、`gold`はその額以上の所持を判定します。

効果は `hp`, `focus`, `gold`, `tide`（整数の増減）、`items`（IDと数量の増減）、`flags`, `clues`（取得するIDの配列）。節の`enter`は初回だけ、`repeat`は到達ごとです。`variants`には条件別の段落を順番に並べ、最初に条件を満たしたものを表示します。

探索判定は `test: {stat, dc, success, failure}`。`stat`は`skill`または`agility`、d6を一度振り能力値を足します。名目上の`to`ではなく、実際に判定で選ばれた節をログに記録します。

戦闘定義は `combat: {enemy, win, flee, modifiers}`。`flee`をnullにすると撤退不可。`modifiers`は条件を満たしたとき敵のHP/装甲/技量/回避/威力を増減し、理由をログに表示します。敵に名前・数値・行動順`pattern`・戦利品`loot`を定義します。

今は信頼できるローカルの物語データをPythonから読み込む方式で、ブラウザから任意ファイルをアップロード・実行する機能はありません。

## 再現性と評価

ダイスは各冒険専用のseed付き乱数です。Randomコントローラー用の乱数と、ゲームのダイスを分離しています。**同じシナリオ・seed・同じ行動列**なら同じ結果です。Jevの選択自体をseedで再現するものではありません。

実行記録v2にはシナリオのバージョン/SHA-256、seed、各手の前後の状態、ダイスと結果、結末IDが含まれます。出力は監査・比較用で、自動ロードするセーブ機能はまだありません。

`tools/rpg_scenarios.py`は**経路を知っている回帰テスト**です。正解経路を与えた100 seedsでの到達確認を、Jevのクリア率として扱ってはいけません。モデルに経路を与えず調べるには `run_rpg.py --backend jev` またはブラウザから実行します。
